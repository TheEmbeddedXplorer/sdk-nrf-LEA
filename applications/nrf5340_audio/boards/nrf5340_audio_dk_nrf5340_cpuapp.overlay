/*
 * Copyright (c) 2024 Nordic Semiconductor ASA
 *
 * SPDX-License-Identifier: LicenseRef-Nordic-5-Clause
 */

/*
 / {
    chosen {
        zephyr,app-uart = &uart2;
    };
};

&uart2 {
	status = "okay";
	current-speed = <115200>;

	pinctrl-0 = <&uart2_default>;
	pinctrl-1 = <&uart2_sleep>;
	pinctrl-names = "default", "sleep";
};

&pinctrl {
	uart2_default: uart2_default {
		group1 {
			psels = <NRF_PSEL(UART_TX, 1, 9)>, <NRF_PSEL(UART_RTS, 1, 11)>;
		};
		group2 {
			psels = <NRF_PSEL(UART_RX, 1, 8)>, <NRF_PSEL(UART_CTS, 1, 10)>;
			bias-pull-up;
		};
	};

	uart2_sleep: uart2_sleep {
		group1 {
			psels = <NRF_PSEL(UART_TX, 1, 9)>, <NRF_PSEL(UART_RX, 1, 11)>,
					<NRF_PSEL(UART_RTS, 1, 8)>, <NRF_PSEL(UART_CTS, 1, 10)>;
			low-power-enable;
		};
	};
};
*/
